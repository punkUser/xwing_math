thead
    tr
        th Token
        th Attacker Delta
        th Defender Delta
tbody
    - import std.format;
    - foreach(i, label; exp_token_labels)
        tr
            td= label
            td #{format("%.9f", exp_attack_tokens[i])}
            td #{format("%.9f", exp_defense_tokens[i])}
